<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

  <!-- NOTE: ListPreference's summary should be set to its value by the
       activity code. -->
  <ListPreference
      android:defaultValue="-1"
      android:key="printer_list"
      android:negativeButtonText="@null"
      android:positiveButtonText="@null"
      android:title="@string/pref_title_choose_a_printer" />

  <ListPreference
      android:defaultValue="-1"
      android:key="network_interfaces_list"
      android:negativeButtonText="@null"
      android:positiveButtonText="@null"
      android:title="@string/network_interfaces_list_title"
      android:summary="@string/network_interfaces_list_summary"/>
  
</PreferenceScreen>
